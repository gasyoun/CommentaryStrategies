[pytest]
# H4351 (08-09-2026): the repo's first test harness. `python -m pytest` from the
# repo root runs everything under tests/; CI (.github/workflows/ci.yml, job
# "Pytest") runs the same command on 3.10/3.11/3.12.
testpaths = tests
addopts = -q --tb=short
python_files = test_*.py
