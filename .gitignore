# audit sidecars
.contributing_old.md
.contributing_audit.txt

# python
__pycache__/
*.pyc

# orchestrator scratch
scripts/_rebuild_summary.json

# H2860: the intermediate hand queue is a build by-product of
# build_licence_register_nilakantha.py --emit-hand-queue; the RULINGS it feeds
# (nilakantha_hand_rulings.json) are tracked, the queue is not.
data/licence_register/nilakantha_hand_queue.json

# secrets — never commit
.env
.env.*
node_modules/
test-results/
playwright-report/

# Claude Code local/session files (keep tracked .claude/launch.json)
.claude/settings.local.json
.claude/worktrees/

# H1685: deterministic intermediates — regenerate with scripts/h1685_adjudicate.py;
# the audit trail lives in ledger_final.json (every rule row keeps its rule_id + reason).
data/analysis/h1685_adjudication/ledger.json
data/analysis/h1685_adjudication/packet_*.json

# Review sheets are personal working artifacts, not repo deliverables
# (/review-sheet Phase 3); the screening evidence beside them is regenerated.
review/

# H2832: extracted text and rendered pages of the Princeton Goldman volumes are
# copyrighted working material (© 1996 Princeton University Press). Only the
# inventories, the bake-off metrics and the verse-number join live in the repo.
data/goldman/text/
data/goldman/png/
data/goldman/**/p[0-9][0-9][0-9][0-9].txt
data/goldman/*.pdf
