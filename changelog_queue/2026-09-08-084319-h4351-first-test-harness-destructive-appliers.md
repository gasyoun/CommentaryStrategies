- **H4351 — first test harness: the 10 destructive appliers pinned for idempotence, non-shrink and malformed-input refusal** (Fable 5.1 `claude-fable-5-1`, 08-09-2026). The repo had zero test files across 164 Python modules (rank 2 of Uprava [reports/TEST_GAP_CENSUS_ORGWIDE_08-09-2026.md](https://github.com/gasyoun/Uprava/blob/main/reports/TEST_GAP_CENSUS_ORGWIDE_08-09-2026.md)). New [`pytest.ini`](https://github.com/gasyoun/CommentaryStrategies/blob/main/pytest.ini) + [`tests/`](https://github.com/gasyoun/CommentaryStrategies/tree/main/tests) (55 tests, 11 modules): [`tests/conftest.py`](https://github.com/gasyoun/CommentaryStrategies/blob/main/tests/conftest.py) stages each applier into a throw-away tree and runs it as a real subprocess, so both path conventions (`__file__`-relative and cwd-relative) land in the sandbox; the two scripts that hard-code a Windows checkout (`backfill_grintser_crossrefs.py`, `merge_new_crosstext.py`) get exactly that one line rewritten, asserted present. Fixtures under [`tests/fixtures/appliers/`](https://github.com/gasyoun/CommentaryStrategies/tree/main/tests/fixtures/appliers) are miniature copies of the real column shapes (apparatus `sarga_NN.json`, gate ledger v2, lexical cards + style-pass patches, Phase-2 candidates/decisions, translit residue cards + sheet export, crosstext clusters, decision ledger, Grintser glossary/corpus JSONL); no test reads or writes the live `data/`. Curated floors frozen as literals (gate ledger 126 entries · ch1 58 cards · book 896 notes / 155 В / 170 cross_text · decision ledger 5507 · pilot 16). `test_coverage_manifest.py` fails if any listed applier is never actually invoked. Two real partial-write defects the suite caught and fixed by parsing every input before the first write: [`scripts/backfill_why_proposed.py`](https://github.com/gasyoun/CommentaryStrategies/blob/main/scripts/backfill_why_proposed.py) rewrote sarga 35 before parsing a truncated sarga 36; [`scripts/rebuild_crosstext.py`](https://github.com/gasyoun/CommentaryStrategies/blob/main/scripts/rebuild_crosstext.py) rewrote the book before opening a truncated decision ledger. New [`scripts/curated_floors_check.py`](https://github.com/gasyoun/CommentaryStrategies/blob/main/scripts/curated_floors_check.py) (job `corpus`) is where the floor literals meet `data/`: a live file below its pin reds CI; the pytest suite stays hermetic. Independent pre-merge review caught a mis-pinned kavya floor (stale `_meta.notes_count` 12 vs 18 live records) and two incomplete fixture key sets — fixed in the same PR. CI job `Pytest` in [`.github/workflows/ci.yml`](https://github.com/gasyoun/CommentaryStrategies/blob/main/.github/workflows/ci.yml) now runs the suite unconditionally on 3.10/3.11/3.12. Uncovered-module census (154 remaining, by class and why) recorded in [`.ai_state.md`](https://github.com/gasyoun/CommentaryStrategies/blob/main/.ai_state.md).
