const enc=new TextEncoder();
async function digest(value){const bytes=await crypto.subtle.digest("SHA-256",enc.encode(JSON.stringify(value)));return Array.from(new Uint8Array(bytes),b=>b.toString(16).padStart(2,"0")).join("");}
export async function getDraft(env,reviewer,revision,sarga){const row=await env.DB.prepare("SELECT version, decisions_json, content_hash, updated_at FROM drafts WHERE reviewer=? AND manifest_revision=? AND sarga=?").bind(reviewer,revision,sarga).first();return row?{version:row.version,decisions:JSON.parse(row.decisions_json),content_hash:row.content_hash,updated_at:row.updated_at}:{version:0,decisions:{}};}
export async function putDraft(env,reviewer,revision,sarga,body){if(!body||typeof body.decisions!=="object"||JSON.stringify(body).length>512000)return Response.json({error:"invalid or oversized draft"},{status:413});const current=await getDraft(env,reviewer,revision,sarga);if(Number(body.version)!==Number(current.version))return Response.json({error:"draft conflict",local:body,remote:current},{status:409});const version=current.version+1;const now=new Date().toISOString();const hash=await digest(body.decisions);await env.DB.prepare("INSERT INTO drafts (reviewer,manifest_revision,sarga,version,decisions_json,content_hash,updated_at) VALUES (?,?,?,?,?,?,?) ON CONFLICT(reviewer,manifest_revision,sarga) DO UPDATE SET version=excluded.version,decisions_json=excluded.decisions_json,content_hash=excluded.content_hash,updated_at=excluded.updated_at").bind(reviewer,revision,sarga,version,JSON.stringify(body.decisions),hash,now).run();return Response.json({version,content_hash:hash,updated_at:now});}
